// Escrever uma função que retorna o numero maior entre 2 numeros
// operação ternaria 
function retornaMaior(x, y) {
  return x > y ? x : y;
}
console.log(retornaMaior(10, 2));

// const max2= (x,y) => x > y ? x : y;
// console.log(retornaMaior(10, 2));

